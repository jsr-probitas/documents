{
  "name": "@probitas/docs",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build && npm run build:post",
    "build:post": "node scripts/post-build.js",
    "preview": "vite preview",
    "generate-api": "node scripts/generate-api-docs.js"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "honox": "^0.1.0",
    "marked": "^17.0.1"
  },
  "devDependencies": {
    "@hono/vite-ssg": "^0.1.0",
    "@tabler/icons": "^3.31.0",
    "@types/node": "^22.0.0",
    "pagefind": "^1.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
